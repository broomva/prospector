# Core FastAPI dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1

# DeepAgents and LangChain (let deepagents dictate versions)
deepagents==0.1.0
langchain>=0.3.0
langchain-anthropic>=1.0.0
langchain-openai>=0.3.0
langgraph>=1.0.0

# Database (optional - we're using CSV for now)
# libsql-experimental>=0.0.55
aiosqlite>=0.20.0

# CORS and middleware
python-multipart==0.0.20

# Environment management
python-dotenv==1.0.1

# MCP (Model Context Protocol) integration
langchain-mcp-adapters>=0.1.0
mcp>=1.3.0

# Logging and monitoring
structlog==24.4.0

# Vector search (matching your Mastra setup) - optional for now
# fastembed>=1.0.0
# numpy>=2.2.0
